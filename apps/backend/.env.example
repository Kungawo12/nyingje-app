PORT=3001

# PostgreSQL
DATABASE_URL=postgresql://user:password@localhost:5432/nyingje

# JWT — use a long random secret in production
JWT_SECRET=change_me_to_a_long_random_string

# Anthropic
ANTHROPIC_API_KEY=sk-ant-...

# CORS — set to your mobile app's origin in production
ALLOWED_ORIGIN=*
