# Task 20260505_005: Waitlist Email Storage
**Type:** code
**From:** Claude (Senior Engineer)
**To:** GPT-4.1
**Created:** 2026-05-05

---

## Context

The Nyingje website (`apps/web/`) has a waitlist form in `app/components/Waitlist.jsx` that currently uses `useState` only — it stores nothing. We need to persist submitted emails to the Neon PostgreSQL database via the backend.

**Backend base URL:** `apps/backend/` running on Railway (deployed). For now use a relative API call from the Next.js server action.

---

## Instruction

Produce full file contents for every file below, clearly labeled with path.

### 1. Backend — `src/db/schema.sql`
Add this table to the EXISTING schema (do not remove anything, just append):
```sql
CREATE TABLE IF NOT EXISTS waitlist (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  email TEXT UNIQUE NOT NULL,
  created_at TIMESTAMPTZ DEFAULT NOW()
);
```

### 2. Backend — `src/routes/waitlist.js` (NEW FILE)
```
POST /waitlist
Body: { email }
- Validate email is present and contains "@"
- Insert into waitlist table (parameterized query)
- If duplicate (error code 23505), return 200 with { message: "Already on the list!" }
- On success return 201 with { message: "You're on the list!" }
- No auth required
```

### 3. Backend — `src/index.js`
Add the waitlist route:
```js
const waitlistRoutes = require('./routes/waitlist');
app.use('/waitlist', waitlistRoutes);
```
Produce the full updated file contents.

### 4. Website — `app/components/Waitlist.jsx`
Replace the frontend-only form with one that calls the backend:
- Use a Next.js **Server Action** (`"use server"`) to POST to the backend
- The server action sends `{ email }` to `${process.env.NEXT_PUBLIC_API_URL}/waitlist`
- On success show: "You're on the list! We'll be in touch."
- On already-subscribed show: "You're already on the list!"
- On error show: "Something went wrong. Please try again."
- Keep all existing styling exactly as-is
- The form component stays `"use client"` — only the action is server-side

### 5. Website — `.env.example`
Add:
```
NEXT_PUBLIC_API_URL=https://your-railway-url.railway.app
```
Produce the full updated file.

---

## Technical Requirements
- Backend: parameterized queries only, no string concatenation
- No auth required on `/waitlist` endpoint
- Next.js server action must handle fetch errors gracefully
- Do not change any styling in Waitlist.jsx

---

## Expected Output
Full file contents for every file modified or created, clearly labeled with path.
Notes on decisions made.
## Summary at the end.
