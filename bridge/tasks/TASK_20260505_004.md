# Task 20260505_004: Wire Mobile App to Real Backend API
**Type:** code
**From:** Claude (Senior Engineer)
**To:** GPT-4.1
**Created:** 2026-05-05

---

## Context

The Nyingje mobile app (`apps/mobile/`) is fully built with screens and components but currently uses **mock data everywhere**. The backend (`apps/backend/`) is now live and real. This task replaces all mocks with real API calls.

**Backend base URL:** will be set via an environment/config variable `API_URL` — use `http://localhost:3002` as default for now.

**Relevant backend endpoints:**
- `POST /auth/register` — body: `{ name, email, password }` → returns `{ token, user }`
- `POST /auth/login` — body: `{ email, password }` → returns `{ token, user }`
- `POST /chat` — body: `{ message, conversationId? }` — requires `Authorization: Bearer <token>` header → returns `{ reply, conversationId }`

---

## Instruction

Update the following files. Produce full file contents for each, clearly labeled with path.

### 1. `hooks/useAuth.js`
- Already stores `user` and `token` in AsyncStorage — keep that
- Add an `api` axios instance exported from a new file `lib/api.js` that uses the base URL and auto-attaches the token header
- No other changes needed to useAuth

### 2. `lib/api.js` (NEW FILE)
```
apps/mobile/lib/api.js
```
Create an axios instance:
- `baseURL`: `process.env.EXPO_PUBLIC_API_URL || 'http://localhost:3002'`
- Request interceptor: reads token from AsyncStorage and attaches `Authorization: Bearer <token>` header on every request
- Export as default

### 3. `app/(auth)/login.jsx`
Replace the mock login block with a real call:
```js
const res = await api.post('/auth/login', { email, password });
await login(res.data.user, res.data.token);
```
- Show specific error messages from the API response if available
- Keep all existing styling exactly as-is

### 4. `app/(auth)/register.jsx`
Replace the mock register block with a real call:
```js
const res = await api.post('/auth/register', { name, email, password });
await login(res.data.user, res.data.token);
```
- Show specific error messages from the API response if available
- Keep all existing styling exactly as-is

### 5. `app/(app)/chat.jsx`
Replace the mock send logic with a real call:
```js
const res = await api.post('/chat', { message: text, conversationId });
setConversationId(res.data.conversationId);
// add res.data.reply as assistant message
```
- Add `conversationId` state (initially null, set after first message)
- Keep mock initial messages for display only — they are NOT sent to the API
- Show a simple error message in chat if the API call fails (e.g. add an error bubble)
- Keep all existing styling exactly as-is

### 6. `app.config.js` (NEW FILE — replaces app.json for env support)
```js
export default {
  expo: {
    // copy all fields from app.json here
    extra: {
      apiUrl: process.env.EXPO_PUBLIC_API_URL || 'http://localhost:3002',
    }
  }
}
```

---

## Technical Requirements
- Use `axios` (already in package.json)
- Do NOT change any StyleSheet or visual code
- Handle network errors gracefully — show Alert or inline error message
- `conversationId` must persist across messages in the same chat session (useState is fine)
- The token interceptor in `lib/api.js` must be async (AsyncStorage.getItem is async)

---

## Expected Output
Full file contents for every file modified or created, clearly labeled with path.
Notes on any decisions made.
## Summary at the end.
