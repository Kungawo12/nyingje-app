# Task 20260505_006: Wire Backend URL into Website and Mobile
**Type:** code
**From:** Claude (Senior Engineer)
**To:** GPT-4.1
**Created:** 2026-05-05

---

## Context

The Nyingje backend is now deployed at:
**`https://backend-ivory-eight-83.vercel.app`**

Two places need this URL wired in:
1. The Next.js website (`apps/web/`) needs `NEXT_PUBLIC_API_URL` so the waitlist form can POST to the backend
2. The mobile app (`apps/mobile/`) needs `EXPO_PUBLIC_API_URL` so login/register/chat work on real devices

---

## Instruction

Produce full file contents for both files below, clearly labeled with path.

### 1. `apps/web/.env.local` (NEW FILE)
```
NEXT_PUBLIC_API_URL=https://backend-ivory-eight-83.vercel.app
```

### 2. `apps/mobile/.env` (NEW FILE)
```
EXPO_PUBLIC_API_URL=https://backend-ivory-eight-83.vercel.app
```

### 3. Update `apps/web/.env.example`
Add this line (do not remove existing content):
```
NEXT_PUBLIC_API_URL=https://your-backend-url.vercel.app
```
Produce the full updated file.

### 4. Update `apps/mobile/.gitignore` (NEW FILE)
Create a .gitignore in apps/mobile/ to make sure .env is never committed:
```
.env
.env.local
node_modules/
.expo/
dist/
```

---

## Important Notes
- `.env.local` and `.env` must NOT be committed to git (they contain real URLs)
- The `.gitignore` files protect them
- Do not change any other files

---

## Expected Output
Full file contents for all 4 files, clearly labeled with path.
## Summary at the end.
