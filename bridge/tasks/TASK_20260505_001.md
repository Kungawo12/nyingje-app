# Task 20260505_001: Scaffold Nyingje App — Mobile + Backend
**Type:** code
**From:** Claude (Senior Engineer)
**To:** GPT-4.1
**Created:** 2026-05-05

---

## Context

We are building Nyingje App — a compassionate AI companion rooted in Buddhist wisdom and modern psychology.
It helps people deal with anger, anxiety, depression, and everyday emotional struggles.

**Stack:**
- Mobile: React Native + Expo (SDK 51+)
- Backend: Node.js + Express
- Database: PostgreSQL
- AI: Claude API (Anthropic)

---

## Instruction

Scaffold the complete initial project structure for both apps. Do NOT use Expo Go's managed workflow
for everything — we want a clean bare workflow but still use EAS. Here is exactly what I need:

### 1. Backend (`apps/backend/`)

Produce the full file contents for:

```
apps/backend/
├── package.json          (express, pg, bcryptjs, jsonwebtoken, dotenv, cors, helmet)
├── .env.example
├── src/
│   ├── index.js          (Express app entry, middleware setup)
│   ├── config/
│   │   └── db.js         (PostgreSQL pool connection)
│   ├── middleware/
│   │   └── auth.js       (JWT verification middleware)
│   ├── routes/
│   │   ├── auth.js       (POST /register, POST /login, POST /refresh)
│   │   └── chat.js       (POST /chat — proxies to Claude API, saves to DB)
│   └── db/
│       └── schema.sql    (users table, conversations table, messages table — pgcrypto encrypted)
```

### 2. Mobile (`apps/mobile/`)

Produce the full file contents for:

```
apps/mobile/
├── package.json          (expo, react-native, expo-router, @react-native-async-storage/async-storage, axios)
├── app.json              (Expo config)
├── app/
│   ├── _layout.jsx       (root layout with auth state)
│   ├── index.jsx         (redirects to /chat or /auth)
│   ├── (auth)/
│   │   ├── _layout.jsx
│   │   ├── login.jsx
│   │   └── register.jsx
│   └── (app)/
│       ├── _layout.jsx
│       └── chat.jsx      (main chat screen — connects to backend /chat)
├── components/
│   ├── ChatBubble.jsx
│   └── ChatInput.jsx
└── hooks/
    └── useAuth.js        (token storage and auth state)
```

### Design Notes
- Chat screen should feel calm and minimal — white/off-white background, soft blue for user bubbles, warm gray for AI bubbles
- No heavy UI libraries — use StyleSheet only for now
- Auth screens should be clean, no clutter
- The backend `/chat` endpoint should maintain conversation history (last 10 messages) per user session

### Security Requirements
- Passwords hashed with bcryptjs (salt rounds: 12)
- JWTs signed with RS256 or HS256 (HS256 is fine for now, note where to upgrade)
- All DB queries use parameterized statements (no string concatenation)
- Helmet.js on express for security headers
- CORS locked to mobile app origin in production

---

## Expected Output

- Full file contents for every file listed above, clearly labeled with their path
- The schema.sql should include pgcrypto extension and encrypted conversation storage
- Notes on any assumptions made
- ## Summary at the end listing every file produced
